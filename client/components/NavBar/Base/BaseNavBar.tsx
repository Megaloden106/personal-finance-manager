import React, { SFC } from 'react';
import styles from './BaseNavBar.scss';

const BaseNavBar: SFC = () => (
  <div className={styles.container}>
    <div className={styles.content}>
      NAV BAR
    </div>
  </div>
);

export default BaseNavBar;
